var User = require('../models/User.js');

module.exports = {

  // show spotify on api/spotify....we will to add here, not to mention
  //the YouTube API
  	spotifyIndex: function(req,res){
  			if(err) return console.log(err)
  			res.json(spotify)
  		})
  	}



} //end
